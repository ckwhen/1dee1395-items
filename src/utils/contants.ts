export enum ButtonTypes {
  DEFAULT = 'default',
  PRIMARY = 'primary',
}

export const MAX_VISIBLE_PAGES: number = 5;

export const PAGINATION_OFFSET: number = 0;

export const PAGE_LIMIT: number = 10;
